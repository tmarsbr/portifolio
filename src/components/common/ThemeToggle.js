/**
 * ThemeToggle — Dark Premium Glass
 * Componente mantido como no-op para compatibilidade.
 * Retorna null pois o tema é sempre dark.
 */

const ThemeToggle = () => null;

export default ThemeToggle;
